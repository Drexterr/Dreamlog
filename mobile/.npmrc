# This project has a pre-existing react/react-dom peer conflict (see CLAUDE.md:
# npm install requires --legacy-peer-deps). EAS build runs `npm ci`, which does
# strict peer validation and fails without this flag. Setting it here makes
# local installs and EAS cloud installs use the same policy.
legacy-peer-deps=true
