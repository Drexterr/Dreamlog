# Sentry source-map upload config (read by android/app/build.gradle's
# sentry.gradle hook). No secrets here - the auth token comes from the
# SENTRY_AUTH_TOKEN env var (EAS secret).
defaults.url=https://de.sentry.io/
defaults.org=bharat-jain-i5
defaults.project=react-native
